# Dependencies
node_modules/

# Build output
dist/
.astro/

# Brainstorming artifacts (visual companion sessions)
.superpowers/

# Local agent skill installations (not portfolio content)
.agents/

# OneDrive sync artifacts
.DS_Store

# OS noise
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Sensitive YAML — master with real client names, NEVER commit
data/portfolio.local.yml
data/private.yml

# Whitelabel mappings — real → anonymized names, NEVER commit.
# Bootstrap from scripts/whitelabel.secrets.example.json (template only).
scripts/whitelabel.secrets.local.json

# (NEXT.md and docs/superpowers/ ARE committed — but only in anonymized
#  form. Run scripts whitelabel logic over any dev notes that mention real
#  client/product/persona names before committing them.)

# Test artifacts
test-results/
playwright-report/
playwright/.cache/
coverage/

# Env
.env
.env.local
.env.*.local

video/

# Voxel preview renders (verification only; .vox files in public/vox ARE committed)
docs/voxel-previews/

# Python bytecode (voxel builders + scripts)
__pycache__/
*.pyc
