# Dependencies
node_modules/

# Build output
dist/
.astro/

# Brainstorming artifacts (visual companion sessions)
.superpowers/

# Local agent skill installations (not portfolio content)
.agents/

# OneDrive sync artifacts
.DS_Store

# OS noise
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Sensitive YAML — master with real client names, NEVER commit
data/portfolio.local.yml
data/private.yml

# Whitelabel mappings — real → anonymized names, NEVER commit.
# Bootstrap from scripts/whitelabel.secrets.example.json (template only).
scripts/whitelabel.secrets.local.json

# Private dev artifacts (brainstorming notes, plan files, handoff docs).
# These referenced real client / product / persona names during development
# and should never enter the public repo.
NEXT.md
docs/superpowers/

# Test artifacts
test-results/
playwright-report/
playwright/.cache/
coverage/

# Env
.env
.env.local
.env.*.local
